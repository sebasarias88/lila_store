/**
 * Offset desde el top del viewport para paneles sticky del catálogo.
 * Barra de anuncios (36px) + navbar desktop (64px) + respiro visual (20px).
 */
export const CATALOG_STICKY_TOP_PX = 120

/** Clase Tailwind equivalente — usar en `sticky` CSS nativo. */
export const CATALOG_STICKY_TOP_CLASS = 'top-[7.5rem]'
